version: 0.2

phases:
  install:
    commands:
      - echo "No dependencies to install"
  build:
    commands:
      - echo "Skipping build — static files only"
artifacts:
  base-directory: frontend
  files:
    - index.html
    - script.js
    - style.css
