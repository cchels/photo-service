version: 0.0
resources:
  - index-photos:
      type: "AWS::Lambda::Function"
      properties:
        name: "index-photos"
        alias: "PROD"
        currentversion: "{{CURRENT}}"
        targetversion: "{{TARGET}}"

hooks: []
